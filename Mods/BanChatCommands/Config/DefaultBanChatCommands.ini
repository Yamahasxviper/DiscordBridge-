[/Script/BanChatCommands.BanChatCommandsConfig]
# BanChatCommands - Admin Configuration
# =====================================
# Note: this file ships with the mod and may be replaced when you update
#   BanChatCommands. To keep your admin list permanently, place your overrides
#   in ONE of:
#   1. Saved/BanChatCommands/BanChatCommands.ini  <- written by BanChatCommands on
#                                                    every start; survives mod wipes
#   2. Saved/Config/<Platform>/BanChatCommands.ini <- standard UE config override
# Both use the same [/Script/BanChatCommands.BanChatCommandsConfig] section header.
# Restart the server after editing any config file.

# -- ADMIN LIST ---------------------------------------------------------------

# EOS Product User IDs -- each +AdminEosPUIDs= entry grants that player
# permission to run the following commands from in-game chat:
#
#   /ban, /tempban, /unban, /unbanname, /banname, /bancheck, /banlist,
#   /banreason, /extend, /scheduleban, /qban, /bulkban,
#   /linkbans, /unlinkbans, /warn, /warnings, /clearwarns, /clearwarn,
#   /mute, /unmute, /mutecheck, /mutelist, /mutereason,
#   /announce, /stafflist, /staffchat, /note, /notes, /reason, /history,
#   /playerhistory, /reputation, /freeze, /clearchat, /reloadconfig
#
#   Moderator-only commands (/kick, /modban, /tempunmute, /mutecheck):
#   controlled separately via +ModeratorEosPUIDs= below.
#
# EOS PUIDs are 32-character hex strings (case-insensitive).
# Players can find their own PUID by running /whoami in-game.
#
# Note: On CSS Dedicated Server all players are identified by their EOS Product
# User ID regardless of their launch platform (Steam, Epic Games Store, etc.).
#
# If the list is empty, those commands are only usable from the server console.
# /whoami is always available to all connected players regardless of this setting.
# The server console always bypasses the admin list.
#
# Example:
#   [/Script/BanChatCommands.BanChatCommandsConfig]
#   +AdminEosPUIDs=00020aed06f0a6958c3c067fb4b73d51
#   +AdminEosPUIDs=aabbccdd11223344aabbccdd11223344
# +AdminEosPUIDs=YOUR_EOS_PUID_HERE
# +ModeratorEosPUIDs=MODERATOR_EOS_PUID_HERE

# -- SETTINGS -----------------------------------------------------------------

BanListPageSize=10
# Number of bans shown per page in /banlist output. Default: 10.

ModBanDurationMinutes=30
# Ban duration in minutes for /modban (moderator-tier bans). Default: 30.

MaxModMuteDurationMinutes=0
# Maximum mute duration (minutes) a moderator may pass to /tempmute.
# 0 = no ceiling (same as admins). Default: 0.

bAllowModNotes=False
# Set to True to allow moderators to use /note and /notes. Default: only admins.

WarningCheckCooldownSeconds=0
# Cooldown in seconds between /warnings / /bancheck uses for non-admin senders.
# 0 = disabled. Default: 0.

# Discord webhook URL for /report command notifications. Leave empty to disable.
# ReportWebhookUrl=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

bCreateWarnOnKick=False
# When True, /kick also records a warning to the player's history. Default: False.

# Discord webhook URL called when /reloadconfig is used. Leave empty to disable.
# ReloadConfigWebhookUrl=

AdminBanRateLimitCount=0
# Maximum ban actions an admin may issue within AdminBanRateLimitMinutes. 0 = disabled.

AdminBanRateLimitMinutes=5
# Time window in minutes for the admin ban rate limit. Default: 5.
